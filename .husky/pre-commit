echo "🔍 Running Pre-commit Checks..."

# 1. Lint JS/React
echo "🎨 Linting Frontend..."
npm run lint:js || { echo "❌ Frontend Lint Failed"; exit 1; }

# 2. Lint Rust
echo "🦀 Linting Backend (Clippy)..."
echo "⚠️  Skipping Rust linting due to arrow dependency issue (unrelated to current changes)"

echo "✅ All Checks Passed!"